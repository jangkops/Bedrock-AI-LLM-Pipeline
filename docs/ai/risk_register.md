# Risk Register: Bedrock Access Control Gateway

> Phase 2 artifact. Updated: 2026-03-23. R26 added (Phase 3 pre-registration).
> Identity model re-baselined: per-user assume-role (`BedrockUser-<username>`) is primary.
> Phase 2 DEPLOYED AND VERIFIED (2026-03-20). All C1-C9 PASS. See `docs/ai/phase2-dev-validation-report.md`.
> Phase 3 DEPLOYED AND VERIFIED (2026-03-23). R24/R25 RESOLVED AND DEPLOYED. See `docs/ai/phase3-dev-validation-report.md`.
> Task 2 bypass prevention COMPLETE (2026-03-19). shlee exception: direct-use, deny removed.

| # | Risk | Severity | Likelihood | Mitigation | Status |
|---|------|----------|------------|------------|--------|
| R1 | Principal normalization 가정이 실제와 다름 | High | Medium | Task 3 discovery에서 랩탑(per-user role assume)+FSx(기존 [default] profile) 두 환경 모두 캡처 후 확정. 한 환경만으로 확정 금지. Per-user role name 기반 정규화 후보 F 추천. | Single-user live-verified (cgjang FSx, 2026-03-17). `derived_principal_id` = `107650139384#BedrockUser-cgjang` live 확인. ARN 구조, role name 안정성, session name 비결정성 확인. Discovery Lambda 재배포 완료. Cross-env (C2), cross-role (C3), fail-closed (C5) deferred. |
| R2 | SCP 적용 승인 지연 | Medium | Medium | v1은 IAM deny로 시작. SCP는 별도 승인 트랙. | Open |
| R3 | 동시 요청 시 쿼터 초과 (race condition) | Low | Medium | v1: post-call ADD, 최대 1회 호출분 초과 허용. v2에서 pre-reservation 검토. | Accepted (v1) |
| R4 | Converse API 미지원 모델 존재 | Medium | Low | v1에서 명시적 에러 반환. InvokeModel은 v2. PrincipalPolicy의 allowed_models에 Converse 지원 모델만 등록하도록 admin 가이드. | Mitigated |
| R5 | RequestLedger PutItem 실패 시 감사 로그 누락 | High | Low | deny + CloudWatch 알람. 운영자 수동 확인. | Mitigated |
| R6 | DailyUsage ADD 실패 (Bedrock 성공 후) | Medium | Low | 응답 반환 + CloudWatch 알람. 운영자 수동 보정. | Mitigated |
| R7 | SES 발송 실패로 admin 알림 미전달 | Low | Low | ApprovalRequest는 저장됨. admin이 UI에서 직접 확인 가능. | Accepted |
| R8 | ApprovalRequest pending 중복 | Low | Low | Race-safe 구현: 별도 ApprovalPendingLock 테이블(PK: principal_id)에 conditional PutItem(attribute_not_exists)으로 원자적 lock 획득. GSI query + PutItem 비원자성 문제 해소. 승인/거부 시 admin API가 lock 삭제. TTL(7일) safety net으로 고아 lock 자동 만료. Locked decision #10. Req 5.3 준수. | Mitigated |
| R9 | Terraform state 손상/유실 | High | Low | S3 backend + DynamoDB lock + versioning. | Mitigated |
| R10 | backend-admin IAM role 확장 승인 지연 | Medium | Medium | Task 11 시작 전까지 승인 필요. 병렬로 승인 요청 진행. | ✅ RESOLVED (2026-03-23) — `mg-infra-admin`에 DynamoDB read 권한 추가됨. Phase 4 Scope A 런타임 검증 완료. |
| R11 | FSx per-user credential 수정 필요 시 미승인 | Low | Low | Per-user assume-role 모델에서는 FSx 사용자가 이미 `[default]` profile에 per-user role 설정 완료. Task 3 FSx 캡처(C2)는 기존 config 읽기만 필요 — credential 파일 수정 불필요. `execute-api:Invoke` 권한 추가만 필요 (role policy 수정, config 파일 수정 아님). Severity/Likelihood 대폭 하향. | Largely mitigated — per-user role 모델 전환으로 |
| R12 | 기존 공유 인프라 의도치 않은 수정 | High | Low | devops-operating-model.md에 non-disruption policy 명시. approval-gate-check + infra-protection + fsx-credential-protection 훅으로 경고. Terraform apply/Ansible run은 승인 필수. | Mitigated |
| R13 | FSx 홈 디렉토리 권한 과다로 config 노출 | Medium | Medium | Per-user role 모델에서는 SSO cache 파일 없음 — `credential_source = Ec2InstanceMetadata`로 EC2 metadata에서 직접 credential 획득. `~/.aws/config`만 보호하면 됨. 홈 디렉토리 700 강제. 이전 SSO token 노출 리스크(F1, F3, F6) 해소. | Partially mitigated — per-user role 모델 전환으로 severity 하향 |
| R14 | root/sudo 사용자의 타 사용자 FSx credential 접근 | Medium | Low | shared host 본질적 한계. sudo 접근 최소화, 감사 로그. 완전 제거 불가. | Accepted |
| R15 | ~~동일 사용자가 여러 permission set으로 로그인 시 principal_id 불일치~~ | ~~Medium~~ | ~~Low~~ | Per-user role 모델에서는 해당 없음. 사용자당 1개 role (`BedrockUser-<username>`)이 고정 매핑. Role 매핑은 root/admin만 관리. | Closed — per-user role 모델 전환으로 해소 |
| R16 | Tasks 4-9 코드가 Task 3 discovery 전에 구현됨 — provisional principal_id | Medium | ~~High~~ Low | normalize_principal_id() Candidate F 구현 및 live 검증 완료 (2026-03-17). `<account>#<role-name>` 형식. 11 unit tests pass. Discovery Lambda 재배포 완료 — `derived_principal_id` live 확인. C2/C3/C5 캡처는 deferred validation follow-up. 배포 차단 부분 해제. | Largely mitigated — normalization 구현 및 live 검증 완료. C2/C3/C5 deferred. |
| R17 | Per-user role에 execute-api:Invoke 권한 추가 시 기존 Bedrock 직접 호출 권한과 공존 | Medium | ~~High~~ Low | 기존 BedrockUser-* role은 Bedrock 직접 호출 권한을 가지고 있을 수 있음. Gateway 전환 후 직접 호출 권한 제거 필요. **Task 2 COMPLETE (2026-03-19)**: `DenyDirectBedrockInference` inline policy applied to all 6 `BedrockUser-*` roles. Direct Bedrock calls denied (`AccessDeniedException`). **Exception: BedrockUser-shlee — deny removed per operator approval. shlee uses direct Bedrock (not gateway).** See `docs/ai/task2-bypass-prevention-execution.md`. | Closed — Task 2 완료. shlee 예외는 의도적 (별도 승인). |
| R18 | Accidental wildcard/prefix authorization — principal_id를 family-level key로 오해하여 `BedrockUser-*` 전체에 동일 정책 적용 | High | Low | Normalized principal_id는 exact per-principal key (`<account>#<full-role-name>`). Policy/quota/approval lookup은 exact match only. Wildcard/prefix/suffix/contains 매칭 금지. DynamoDB GetItem은 본질적으로 exact-match. Admin UI/API에서 bulk policy 생성 시에도 per-principal 개별 레코드 생성 필수. Req 2.7 명시. | Mitigated by design |
| R19 | Derived username과 enforcement identity 간 drift — display용 username이 enforcement에 사용되어 다른 사용자의 정책이 적용됨 | Medium | Low | Derived username (`cgjang`)은 display/reporting metadata only (Req 2.9). Enforcement identity는 `<account>#<full-role-name>` 전체. Admin이 PrincipalPolicy 생성 시 full principal_id를 사용해야 함. Admin UI에서 derived username을 편의 표시하되, 저장/조회 키는 반드시 full principal_id. | Mitigated by design — admin UI 구현 시 재검증 필요 |
| R20 | Session name 가정 과신 — Task 3 캡처 전에 session name 형태를 확정하여 잘못된 정규화 규칙 적용 | Medium | Medium | Session name은 `credential_source = Ec2InstanceMetadata` 사용 시 EC2 instance ID일 수 있고, 랩탑에서 `aws sts assume-role` 시 사용자 지정 값일 수 있음. 후보 F는 session name에 의존하지 않으므로 이 리스크는 완화됨. 단, Task 3 캡처 전까지 session name 형태에 대한 어떤 가정도 확정하지 않음. | Single-user live-verified — C1 evidence: FSx session name = `botocore-session-<epoch>` (SDK-generated, NOT EC2 instance ID). A1 가정 부분 반증. Candidate F는 session name 제외하므로 영향 없음. Live smoke test (session `botocore-session-1773807868`)에서 `derived_principal_id` = `107650139384#BedrockUser-cgjang` 확인 — session name 정상 제외됨. Laptop session name 패턴은 C2에서 확인 필요 (deferred). |
| R21 | Accidental persistence of full assumed-role ARN instead of normalized principal_id | High | ~~Medium~~ Low | `normalize_principal_id()` Candidate F 구현 및 live 검증 완료 (2026-03-17). Full userArn 대신 `<account>#<role-name>` 반환. Session name 제외됨. 11 unit tests로 session suffix 무시, fail-closed, cross-role isolation 검증. Discovery Lambda 재배포 완료 — live smoke test에서 `derived_principal_id` = `107650139384#BedrockUser-cgjang` 확인. | Mitigated — normalization 구현 및 live 검증으로 해소. Placeholder 동작 완전 제거됨. |
| R22 | ModelPricing staleness — admin이 pricing 업데이트를 잊으면 실제 비용과 enforcement 비용 간 drift 발생 | Medium | Medium | Q1 결정 (2026-03-18): fixed KRW rates in ModelPricing table, admin-managed. Admin이 AWS pricing 변경 또는 환율 변동 시 수동 업데이트 필요. Mitigation: (1) runbook에 월간 pricing 점검 절차 추가, (2) ModelPricing `effective_date` 필드로 staleness 감지 가능, (3) v2에서 pricing staleness CloudWatch alarm 검토. | Open — operational procedure required |
| R23 | Approver email domain unverified — `changgeun.jang@mogam.er.kr` 도메인 `mogam.er.kr`이 한국 조직에 비정상적 (`.er.kr`은 드문 ccTLD, `.or.kr` 또는 `.co.kr`이 일반적) | Medium | Medium | SES 배포 전 실제 도메인 확인 필수. 잘못된 도메인으로 SES verified sender 설정 시 이메일 발송 실패. Mitigation: (1) 운영자에게 정확한 도메인 확인 요청, (2) SES 설정은 도메인 확인 후에만 진행. | Open — domain verification pending |
| R24 | ~~`_end_of_month_ttl()` UTC→KST 미수정 상태에서 boost 생성 시 TTL이 최대 9시간 조기 만료~~ | ~~Low~~ | ~~Medium~~ | `gateway_approval.py:_end_of_month_ttl()` KST fix applied (2026-03-22). `timezone.utc` → `KST = timezone(timedelta(hours=9))`. Docstring + SES email text updated. | ✅ RESOLVED AND DEPLOYED (2026-03-23) — TTL `1774969199` = 2026-03-31T23:59:59 KST verified |
| R25 | ~~Lambda `handle_approval_request()` 입력 검증 부재로 잘못된 approval request 생성 가능~~ | ~~Medium~~ | ~~Medium~~ | Phase 3 validation applied (2026-03-22): V1 reason non-empty, V2 `requested_increment_krw == 500000`, V3 hard cap pre-validation (`effective + 500K ≤ 2M`). All validation runs BEFORE lock acquisition to avoid orphan locks. | ✅ RESOLVED AND DEPLOYED (2026-03-23) — AC1/AC2/AC5 all PASS |
| R26 | Approval endpoint 인가 — `POST /api/gateway/approval` 호출 권한이 admin role로 제한되지 않으면 일반 사용자가 자신의 한도를 승인할 수 있음 | High | Low | `@admin_required` 데코레이터 이미 적용됨 (`gateway_approval.py`). Phase 3 구현 시 유지 검증. | Mitigated — 기존 코드 확인됨 |
